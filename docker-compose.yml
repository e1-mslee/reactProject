services:
  react-prj-server:
    build: ./frontend
    ports:
      - 80:5173
    volumes:
      - /var/lib/jenkins/workspace/UDA/node_modules:/app/node_modules
  spring-prj-server:
    build: ./backend
    ports:
      - 8080:8080
# docker compose up -d --build
# docker compose down
